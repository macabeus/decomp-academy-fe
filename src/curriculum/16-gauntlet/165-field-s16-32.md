---
id: gauntlet-field-s16-32
title: Read a s16 field at offset 32
difficulty: 3
concepts:
  - structs
  - loads
  - s16
symbol: read
hints:
  - A s16 loads with lha.
  - The field sits at offset 32, so expect a displacement of 32.
  - Just `return s->field;`.
---

# Load a `s16` at byte offset 32

A typed field access compiles to a single load with the field's displacement.
A `s16` field reads with **`lha`** at displacement 32. The *type* picks the load opcode; the *offset* picks the displacement.

```c
typedef struct { u8 _pad[32]; s16 field; } S;
```

## Your task
Write `read` to reproduce the assembly above.

<!-- starter -->
```c
s16 read(S* s) {
    return 0;
}
```

<!-- solution -->
```c
s16 read(S* s) {
    return s->field;
}
```

<!-- context -->
```c
typedef struct { u8 _pad[32]; s16 field; } S;
```
