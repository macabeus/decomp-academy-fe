---
id: gauntlet-shr-9
title: Unsigned shift right by 9
difficulty: 1
concepts:
  - shifts
  - unsigned
symbol: shr
hints:
  - Unsigned right shift by 9 is `srwi r3, r3, 9`.
  - Write `x >> 9`.
---

# Logical shift right by 9

On an unsigned value, `x >> 9` is **`srwi r3, r3, 9`** — high bits filled
with zero. (On a *signed* value the compiler would use `srawi` to preserve the
sign instead.)

## Your task
Write `shr` on a `u32` to match the target.

<!-- starter -->
```c
u32 shr(u32 x) {
    // your code here
    return 0;
}
```

<!-- solution -->
```c
u32 shr(u32 x) {
    return x >> 9;
}
```
