---
id: gauntlet-field-u16-12
title: Read a u16 field at offset 12
difficulty: 3
concepts:
  - structs
  - loads
  - u16
symbol: read
hints:
  - A u16 loads with lhz.
  - The field sits at offset 12, so expect a displacement of 12.
  - Just `return s->field;`.
---

# Load a `u16` at byte offset 12

A typed field access compiles to a single load with the field's displacement.
A `u16` field reads with **`lhz`** at displacement 12. The *type* picks the load opcode; the *offset* picks the displacement.

```c
typedef struct { u8 _pad[12]; u16 field; } S;
```

## Your task
Write `read` to reproduce the assembly above.

<!-- starter -->
```c
u16 read(S* s) {
    return 0;
}
```

<!-- solution -->
```c
u16 read(S* s) {
    return s->field;
}
```

<!-- context -->
```c
typedef struct { u8 _pad[12]; u16 field; } S;
```
