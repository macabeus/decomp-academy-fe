---
id: gauntlet-field-u16-0
title: Read a u16 field at offset 0
difficulty: 2
concepts:
  - structs
  - loads
  - u16
symbol: read
hints:
  - A u16 loads with lhz.
  - The field sits at offset 0, so expect a displacement of 0.
  - Just `return s->field;`.
---

# Load a `u16` at byte offset 0

A typed field access compiles to a single load with the field's displacement.
A `u16` field reads with **`lhz`** at displacement 0. The *type* picks the load opcode; the *offset* picks the displacement.

```c
typedef struct { u16 field; } S;
```

## Your task
Write `read` to reproduce the assembly above.

<!-- starter -->
```c
u16 read(S* s) {
    return 0;
}
```

<!-- solution -->
```c
u16 read(S* s) {
    return s->field;
}
```

<!-- context -->
```c
typedef struct { u16 field; } S;
```
