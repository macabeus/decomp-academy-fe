---
id: gauntlet-field-s32-24
title: Read a s32 field at offset 24
difficulty: 3
concepts:
  - structs
  - loads
  - s32
symbol: read
hints:
  - A s32 loads with lwz.
  - The field sits at offset 24, so expect a displacement of 24.
  - Just `return s->field;`.
---

# Load a `s32` at byte offset 24

A typed field access compiles to a single load with the field's displacement.
A `s32` field reads with **`lwz`** at displacement 24. The *type* picks the load opcode; the *offset* picks the displacement.

```c
typedef struct { u8 _pad[24]; s32 field; } S;
```

## Your task
Write `read` to reproduce the assembly above.

<!-- starter -->
```c
s32 read(S* s) {
    return 0;
}
```

<!-- solution -->
```c
s32 read(S* s) {
    return s->field;
}
```

<!-- context -->
```c
typedef struct { u8 _pad[24]; s32 field; } S;
```
