---
id: gauntlet-field-s16-8
title: Read a s16 field at offset 8
difficulty: 3
concepts:
  - structs
  - loads
  - s16
symbol: read
hints:
  - A s16 loads with lha.
  - The field sits at offset 8, so expect a displacement of 8.
  - Just `return s->field;`.
---

# Load a `s16` at byte offset 8

A typed field access compiles to a single load with the field's displacement.
A `s16` field reads with **`lha`** at displacement 8. The *type* picks the load opcode; the *offset* picks the displacement.

```c
typedef struct { u8 _pad[8]; s16 field; } S;
```

## Your task
Write `read` to reproduce the assembly above.

<!-- starter -->
```c
s16 read(S* s) {
    return 0;
}
```

<!-- solution -->
```c
s16 read(S* s) {
    return s->field;
}
```

<!-- context -->
```c
typedef struct { u8 _pad[8]; s16 field; } S;
```
