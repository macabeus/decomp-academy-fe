---
id: gauntlet-fmul-1000
title: Float multiply by 100.0f
difficulty: 2
concepts:
  - floats
  - fmuls
  - literals
symbol: scale
hints:
  - The constant loads with `lfs`, the multiply is `fmuls`.
  - Write `x * 100.0f`.
---

# Scale a float by 100.0f

The literal `100.0f` is loaded from the small-data area with **`lfs`**, then a
single-precision **`fmuls`** does the work. Keeping everything `f32` avoids a
`frsp` rounding step.

## Your task
Write `scale` so it compiles to the `fmuls` above.

<!-- starter -->
```c
f32 scale(f32 x) {
    return 0.0f;
}
```

<!-- solution -->
```c
f32 scale(f32 x) {
    return x * 100.0f;
}
```
