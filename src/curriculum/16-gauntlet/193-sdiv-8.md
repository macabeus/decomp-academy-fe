---
id: gauntlet-sdiv-8
title: Signed divide by 8
difficulty: 3
concepts:
  - strength-reduction
  - signed
  - srawi
  - addze
symbol: sdiv
hints:
  - Signed divide by 8 is `srawi` then `addze`, not a lone shift.
  - Write `x / 8` on an s32.
---

# Signed divide by 8

A *signed* divide by a power of two can't be a plain shift: C rounds toward zero
but an arithmetic shift rounds toward negative infinity, so MWCC adds a fixup.
The signature is **`srawi r0, r3, 3`** followed by **`addze r3, r0`** — the
`addze` nudges the quotient back toward zero for negative inputs.

## Your task
Write `sdiv` on an `s32` so it compiles to the `srawi` + `addze` above.

<!-- starter -->
```c
s32 sdiv(s32 x) {
    // your code here
    return 0;
}
```

<!-- solution -->
```c
s32 sdiv(s32 x) {
    return x / 8;
}
```
